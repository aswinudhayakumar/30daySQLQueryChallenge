
# 30daySQLQueryChallenge

This contains the question and solution for the 30-day SQL query challenge from https://www.youtube.com/@techTFQ.

## Postgres Instance
It contains a docker-compose file to run a Postgres instance.

Run this command to initialize the Postgres instance
```
docker-compose -f .\docker-compose-db.yml up -d
```
